node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.workdir/

# The two engine-source repository packages are published as SEPARATE git
# repositories (see PUBLISHING.md) — never nest them inside the MCP repo.
repos/
