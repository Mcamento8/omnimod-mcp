# CHANGE LOG — `__OMNIMOD_MAP__`

> **This file is owned by AI agents, not by the game.** Append-only: newest entry at the
> bottom. Never delete or rewrite an existing entry — if one is wrong, append a correction
> that cites your evidence. Entry format and required sections:
> `agent/09_HANDOFF_PROTOCOL.md`.

Every task that changes this map gets an entry. An entry must include: what was requested,
how you interpreted it, the batch files with their ops/placed/failed counts, the blocks
used (in their translated 1.8 form), the verification results, the decisions you made that
the user did not explicitly specify, and notes for the next agent.

An agent picking up this map reads this file to learn what exists, why it was built that
way, and what has already been tried and failed. Write it for that reader.

---

## <yyyy-mm-dd hh:mm> — Workspace created

**Agent:** the game engine (automatic)
**What:** the `_dev/` workspace and the agent context pack were provisioned for this
map. No world changes were made.
**Next:** the first agent to work here must fill in `MAP_OVERVIEW.md` and generate
`FILE_MAP.md` before or alongside its first build.

---

<!-- Append new entries below this line. Newest last. -->

<!-- omnimod-docs-version: omnimod-agent-docs-4 -->
